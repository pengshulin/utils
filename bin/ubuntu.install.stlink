#!/bin/bash
# ref to https://github.com/texane/stlik.git

pushd ~

if [[ -d ~/.stlink ]]; then
pushd .stlink
git pull
make clean
else
git clone git://github.com/texane/stlink.git ~/.stlink
pushd .stlink
fi

./autogen.sh
./configure
# TODO: patch 
make
sudo make install
popd
popd

