#!/bin/bash
# run st-util gdbserver looply
#

# counts reload times
reload_counter=0
cmd=st-util

# main loop
while [ 1 ]; do
  echo -e "\e[34m[$reload_counter]" "$cmd\e[0m"
  $cmd
  echo -e "\e[31mStopped.\e[0m"
  reload_counter=$((reload_counter+1)) 
  sleep 0.5  # wait for a while
done

