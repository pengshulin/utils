#!/usr/bin/env bash
# execute commands repeatly

cmd=$@
test -z "$cmd" && echo "usage: repeat_run commands ..." && exit 1

counter=1
while [ 1 ]; do
    echo -e "\e[34m[$counter]" "$cmd\e[0m"
    $cmd
    echo -e "\e[31mStopped.\e[0m"
    counter=$((counter+1)) 
    sleep 0.5
done

